# Copy this file to `.env.local` for local development.
# `.env.local` is the local source of truth.
# `.env.production` must define the same keys explicitly for production builds.
# Allowed feature states: `hidden`, `development`, `live`.
VITE_APP_ENV=local
VITE_API_BASE_URL=http://localhost:4000
VITE_FEATURE_ECONOMIC_CALENDAR=development
